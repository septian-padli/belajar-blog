import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['picsum.photos', 'img.clerk.com', "cebtdatwxbubgfirmtxa.supabase.co"],
  },
};

export default nextConfig;
